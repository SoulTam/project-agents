<!-- 创建时间: 2026-06-22 01:28 -->
<!-- 最后修改: 2026-06-22 01:28 -->

# SP-02: 批量更新现有非代码文档添加时间戳

## 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | SP-02 |
| 名称 | 批量更新现有非代码文档添加时间戳 |
| 类型 | D（批量更新） |
| 对应蓝图章节 | ④技术设计Agent产出 |
| 前置依赖 | SP-01（规则先写入规范） |

## 最终结果（逐字拷贝自结果蓝图）

### 更新文件时的流程（拷贝自结果蓝图④技术设计Agent产出）
```
1. 读取文件 → 获取创建时间行（第1行）
2. $now = Get-Date -Format "yyyy-MM-dd HH:mm"
3. 替换第2行的最后修改时间
4. 保存文件
```

### 识别非代码文档的规则（拷贝自结果蓝图④技术设计Agent产出）
```
排除的文件扩展名：.py .js .ts .java .css .scss .json .yaml .yml .toml .xml .sql .sh .bat .ps1
Doc后缀不包含源代码标识 → 纳入元数据
```

## 验收标准（逐字拷贝自结果蓝图）

### 时间格式与命令（拷贝自结果蓝图④技术设计Agent产出）
- 格式：`yyyy-MM-dd HH:mm`（24小时制）
- 获取命令：PowerShell `Get-Date -Format "yyyy-MM-dd HH:mm"`

## 执行步骤

### 步骤1：查找所有非代码文档
扫描项目中所有 .md 文件（排除源代码目录如 src/、node_modules/ 等）。

### 步骤2：遍历每个文件，添加/更新时间戳
对每个 .md 文件：
- 如已有时间戳头部 → 更新 `最后修改` 为当前时间
- 如无时间戳头部 → 在文件头部插入 `<!-- 创建时间: $now -->` 和 `<!-- 最后修改: $now -->`

### 步骤3：验证覆盖
确认所有非代码文档均已包含时间戳头部，无一遗漏。

## 执行产出物
| 预期产出 | 路径 |
|---------|------|
| 所有非代码文档头部的时间戳 | 项目全部 .md 文件 |
