<!-- 创建时间: 2026-06-22 01:28 -->
<!-- 最后修改: 2026-06-22 01:28 -->

# SP-01: 将元数据规则写入AGENTS.md/输出规范

## 基本信息

| 字段 | 值 |
|------|-----|
| 编号 | SP-01 |
| 名称 | 将元数据规则写入AGENTS.md/输出规范 |
| 类型 | A（规范修改） |
| 对应蓝图章节 | ③功能设计Agent产出 / ④技术设计Agent产出 |
| 前置依赖 | 无 |

## 最终结果（逐字拷贝自结果蓝图）

### 元数据格式（拷贝自结果蓝图③功能设计Agent产出）
每篇文档的第1-2行（两行之间无空行）：
```
<!-- 创建时间: 2026-06-22 01:23 -->
<!-- 最后修改: 2026-06-22 01:23 -->
```

第3行为空行，第4行开始正文。

### 行为规则（拷贝自结果蓝图③功能设计Agent产出）
| 场景 | 行为 |
|------|------|
| 创建新文档 | 自动写入 `创建时间` 和 `最后修改`（两者相同） |
| 更新已有文档 | 修改 `最后修改` 为当前时间，`创建时间` 保持不动 |
| 无文件变更（纯对话） | 不操作 |
| 排除源代码 | 仅对非代码文档执行，源代码不添加 |

## 验收标准（逐字拷贝自结果蓝图）

### 时间格式与命令（拷贝自结果蓝图④技术设计Agent产出）
- 格式：`yyyy-MM-dd HH:mm`（24小时制）
- 获取命令：PowerShell `Get-Date -Format "yyyy-MM-dd HH:mm"`

### 创建文件时的流程
```
1. $now = Get-Date -Format "yyyy-MM-dd HH:mm"
2. 写入文件头：
   <!-- 创建时间: $now -->
   <!-- 最后修改: $now -->
   (空行)
   [文件正文]
```

### 更新文件时的流程（拷贝自结果蓝图④技术设计Agent产出）
```
1. 读取文件 → 获取创建时间行（第1行）
2. $now = Get-Date -Format "yyyy-MM-dd HH:mm"
3. 替换第2行的最后修改时间
4. 保存文件
```

### 识别非代码文档的规则（拷贝自结果蓝图④技术设计Agent产出）
```
排除的文件扩展名：.py .js .ts .java .css .scss .json .yaml .yml .toml .xml .sql .sh .bat .ps1
Doc后缀不包含源代码标识 → 纳入元数据
```

## 执行步骤

### 步骤1：读取 AGENTS.md 当前输出规范章节
读取 AGENTS.md 中"输出规范（所有阶段适用）"章节（约第697行）。

### 步骤2：追加元数据时间戳规则
在输出规范章节中追加一条规则：
"- 所有非代码文档必须在文件最开头包含创建时间和最后修改时间（格式：`<!-- 创建时间: yyyy-MM-dd HH:mm -->` / `<!-- 最后修改: yyyy-MM-dd HH:mm -->`）"

### 步骤3：验证修改
确认规则位置正确、格式与现有规范一致。

## 执行产出物
| 预期产出 | 路径 |
|---------|------|
| 修改后的AGENTS.md（输出规范新增元数据规则） | AGENTS.md |
