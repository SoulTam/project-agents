<!-- 创建时间: 2026-06-22 01:28 -->
<!-- 最后修改: 2026-06-22 01:28 -->

# 执行计划：API使用策略制定

## 目标

制定API使用策略，明确开发Agent在何时使用HTTP API接口、何时使用程序级接口（直接调用或interface定义）。

## 策略内容概要

### 三种接口类型

| 类型 | 定义 | 通信方式 |
|------|------|----------|
| HTTP API | 通过HTTP协议暴露的RESTful接口，跨网络调用 | 网络请求（同步/异步） |
| 程序直接调用 | 同一进程内，一个类直接调用另一个类的方法 | 方法调用 |
| Interface定义 | 通过Java Interface定义契约，实现类可替换，面向接口编程 | 方法调用（多态） |

### 决策规则

| 序号 | 判断条件 | 选择策略 | 原因 |
|------|----------|----------|------|
| 1 | 调用方与被调用方部署在不同服务/进程 | HTTP API | 跨进程必须通过网络通信 |
| 2 | 调用方与被调用方在同一服务同一进程，且调用关系固定不变 | 程序直接调用 | 无需抽象，简单直接 |
| 3 | 调用方与被调用方在同一服务同一进程，但实现可能切换（如支付渠道、存储方式） | Interface定义 | 面向接口编程，支持多实现替换 |
| 4 | 调用方与被调用方在同一服务同一进程，但需要Mock测试或未来可能替换 | Interface定义 | 便于单元测试Mock，预留扩展点 |
| 5 | 同一进程内，模块间有明确领域边界，需解耦 | Interface定义 | 通过接口隔离领域边界，降低耦合 |
| 6 | 前端与后端之间 | HTTP API | 前后端分离架构，跨运行环境 |
| 7 | 同一进程内，工具类/纯函数调用 | 程序直接调用 | 工具方法无状态无多态需求，无需抽象 |
| 8 | 第三方服务/外部系统 | HTTP API | 外部系统通过协议通信 |

### 决策流程

```
调用方和被调用方是否部署在不同服务/进程？
├── 是 → HTTP API
└── 否（同一进程内）
    ├── 是否需要支持多种实现或未来替换？
    │   ├── 是 → Interface定义
    │   └── 否
    │       ├── 是否有明确领域边界需解耦？
    │       │   ├── 是 → Interface定义
    │       │   └── 否
    │       │       ├── 是否需要Mock测试？
    │       │       │   ├── 是 → Interface定义
    │       │       │   └── 否
    │       │       │       ├── 是否为工具类/纯函数？
    │       │       │       │   ├── 是 → 程序直接调用
    │       │       │       │   └── 否 → 程序直接调用
    └── 前端与后端 → HTTP API
    └── 第三方/外部系统 → HTTP API
```

### 示例场景

| 场景 | 选择 | 说明 |
|------|------|------|
| 订单服务调用库存服务 | HTTP API | 不同微服务，跨进程 |
| OrderService调用OrderRepository | Interface定义 | 可能切换实现（JPA/MyBatis），需Mock测试 |
| OrderService调用PaymentService | Interface定义 | 支付渠道可能切换（支付宝/微信），需Mock测试 |
| PaymentService调用AlipayClient | 程序直接调用 | 具体实现类，无替换需求 |
| OrderController调用OrderService | Interface定义 | Service层面向接口，Controller依赖接口非实现 |
| StringUtils.isEmpty() | 程序直接调用 | 工具方法，无状态无多态 |
| 前端页面调用后端接口 | HTTP API | 前后端分离，跨运行环境 |
| OrderService调用LogService | 程序直接调用 | 同进程内日志服务，调用关系固定 |

## 执行步骤

| 序号 | 步骤 | 详细执行方式 |
|------|------|-------------|
| 1 | 编写API使用策略文档 | 在`.ai-team/knowledge-base/api-strategies/`目录下创建`interface-selection-strategy.md`，写入上述三种接口类型定义、8条决策规则、决策流程图、示例场景表 |
| 2 | 更新技术设计Skill | 在`.ai-team/skills/technical-design/technical-design/skill.md`的步骤3"定义API接口"前增加步骤"接口类型决策"，要求：对每个模块间交互点，按策略文档决策规则判断接口类型，在技术设计文档中以表格列出每个交互点的接口类型选择及判断依据 |
| 3 | 更新Java开发Skill | 在`.ai-team/skills/code-dev/java/java-development/skill.md`的步骤7"编写Service层"前增加步骤"接口类型决策"，要求：读取技术设计文档中的接口类型决策结果，Interface类型的创建Interface+Impl两文件，直接调用类型的在调用方直接注入实现类 |
| 4 | 更新`.cursorrules` | 在`.cursorrules`的Skill References表格中增加`interface-selection-strategy | .ai-team/knowledge-base/api-strategies/interface-selection-strategy.md` |

## 待确认

是否同意执行？
