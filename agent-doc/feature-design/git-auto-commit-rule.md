# Git自动提交规则 — 功能设计

## 功能描述
在连续执行多个子计划的过程中，每完成3个子计划自动执行git提交，防止修改文件过多导致commit失败。

## 交互流程
1. 子计划执行完毕 → 完成计数+1
2. 判断计数%3==0 → 是则触发git提交
3. 输出git提交日志到控制台
4. 继续执行下一个子计划

## 验收标准
- SP-01、SP-02完成后不触发git提交
- SP-03完成后触发git commit（计数3%3==0）
- commit message格式正确
- 仅commit不自动push（或根据配置决定）
